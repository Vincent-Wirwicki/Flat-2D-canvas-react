import FakeSphere from "../components/FakeSphere";

const HomePage = () => {
  return (
    <section style={{ width: "50%", height: "75%" }}>
      <FakeSphere />
    </section>
  );
};

export default HomePage;
